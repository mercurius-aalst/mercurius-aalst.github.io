export type Event = {
  imageUrl?: string,
  title: string,
  tldr?: string,
  what: string,
  when: string,
  where: string,
  who: string,
  price: string,
  facebookUrl?: string,
  registerUrl: string,
  mapsUrl: string
  picturesUrl?: string,
} 